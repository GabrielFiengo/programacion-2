// MCD de tres numeros.cpp : Defines the entry point for the console application.
// Autor: Gabriel Fiengo
// Fecha: 15/04/2014

#include "stdafx.h"
#include <iostream>
#include "conio.h"
using namespace std;
void main()
{
int a,b,c,mcd,cont,menor;
cout<<"Ingrese a: ";
cin>>a;
cout<<"Ingrese b: ";
cin>>b;
cout<<"Ingrese c: ";
cin>>c;
if(a<b&&a<c)
  menor=a;
else
  if(b<a&&b<c)
    menor=b;
  else
	  menor=c;
mcd=1;
cont=2;
while(cont<=cont)
{
if((a%cont==0)&&(b%cont==o)&&(c%cont==0)
{
a=a/cont;
b=b/cont;
c=c/cont;
menor=menor/cont;
mcd=mcd*cont;
cont=cont-1;
}
cont=cont+1;
}
cout<<mcd;
getch();
}
